<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Optimización de Buses - Centro Memoria</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f7fa;
      text-align: center;
      padding: 20px;
    }
    form {
      background: white;
      padding: 20px;
      max-width: 400px;
      margin: 30px auto;
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
    }
    input {
      width: 100%;
      margin-top: 5px;
      padding: 8px;
      border-radius: 5px;
      border: 1px solid #ccc;
    }
    button {
      background-color: #007bff;
      color: white;
      padding: 10px;
      border: none;
      border-radius: 5px;
      margin-top: 15px;
      width: 100%;
      cursor: pointer;
    }
    button:hover { background-color: #0056b3; }
    .resultado {
      background-color: #e6f7ff;
      margin-top: 20px;
      padding: 10px;
      border-radius: 5px;
    }
  </style>
</head>
<body>
  <h1>Optimización de Buses - Estación Centro Memoria</h1>

  <form id="form">
    <label>Total de buses:</label>
    <input type="number" id="totalBuses" required>

    <label>Demanda de pasajeros:</label>
    <input type="number" id="demanda" required>

    <button type="submit">Calcular</button>
  </form>

  <div id="resultado" class="resultado"></div>

  <script>
    document.getElementById('form').addEventListener('submit', function(e) {
      e.preventDefault();
      const totalBuses = parseFloat(document.getElementById('totalBuses').value);
      const demanda = parseFloat(document.getElementById('demanda').value);

      // Aquí simulamos el cálculo del método Gauss y el tiempo mínimo
      const cap1 = 160, cap2 = 250, cap3 = 160;
      const A = [
        [1, 1, 1],
        [cap1, cap2, cap3]
      ];
      const b = [totalBuses, demanda];

      // Resolución manual tipo Gauss
      const x3 = 0; // suposición básica para reducir el sistema
      const x2 = (b[1] - b[0]*cap1) / (cap2 - cap1); 
      const x1 = b[0] - x2 - x3;

      // Calcular tiempo mínimo estimado (similar a Símplex)
      const tiempoMin = 6*x1 + 4*x2 + 5*x3;

      document.getElementById('resultado').innerHTML = `
        <h3>Resultado</h3>
        <p>🚌 Buses tipo 1 (articulados): <b>${x1.toFixed(2)}</b></p>
        <p>🚌 Buses tipo 2 (bi-articulados): <b>${x2.toFixed(2)}</b></p>
        <p>🚌 Buses tipo 3 (mixtos): <b>${x3.toFixed(2)}</b></p>
        <p>⏱️ Tiempo mínimo total estimado: <b>${tiempoMin.toFixed(2)} minutos</b></p>
      `;
    });
  </script>
</body>
</html>
